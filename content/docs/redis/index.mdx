---
title: Redis
description: The open source object storage service
icon: Layers
---

import {
    CodeXml,
    Variable,
    Wrench,
    ScrollText,
    Telescope,
    MessageCircleQuestion
} from 'lucide-react';

<Cards>
    <Card icon={<CodeXml className="text-sky-700"/>} title='Configuration' href={"/docs/redis/config"}>
        Configure redis module, and use multiple redis nodes.
    </Card>
    <Card icon={<Variable className="text-amber-500"/>} title='Parameters' href={"/docs/redis/param"}>
        Customize redis components with 21 parameters
    </Card>
    <Card icon={<Wrench className={"text-violet-500"}/>} title='Administration' href={"/docs/redis/admin"}>
        Create, remove, expand, redis cluster
    </Card>
    <Card icon={<ScrollText className="text-blue-400"/>} title='Playbooks' href={"/docs/redis/playbook"}>
        Ansible playbooks that can be used in this module
    </Card>
    <Card icon={<Telescope className="text-orange-600"/>} title='Monitoring' href={"/docs/redis/monitor"}>
        Dashboards, metrics, record & alerting rules.
    </Card>
    <Card icon={<MessageCircleQuestion className="text-emerald-600"/>} title='FAQ' href={"/docs/redis/faq"}>
        Frequently Asked Questions about redis module
    </Card>
</Cards>
