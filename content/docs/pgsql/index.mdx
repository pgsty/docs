---
title: PostgreSQL
description: The most advanced open source relational database in the world!
---


import { getPageTreePeers } from 'fumadocs-core/server';
import { source } from '@/lib/source';

<Cards>

    {getPageTreePeers(source.pageTree, '/docs/pgsql').map((peer) => (
        <Card key={peer.url} title={peer.name} href={peer.url}>
            {peer.description}
        </Card>
    ))}

</Cards>