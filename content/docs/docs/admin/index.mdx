---
title: Administration
description: Manage your deployment
icon: Landmark
---


import {
    TerminalIcon,
    PlayIcon,
    LayoutDashboardIcon, 
    ActivityIcon,
    GlobeIcon,
    ShieldCheckIcon,
    Warehouse,
    DoorOpen,
    Database,
    Landmark,
    CpuIcon,
    Settings,
    Box,
    Layers,
} from 'lucide-react';

<Cards>
    <Card icon={<TerminalIcon className="text-black"/>} title='Ansible' href={"/docs/admin/ansible"}>
        Run admin commands with ansible
    </Card>
    <Card icon={<PlayIcon className="text-blue-500"/>} title='Playbook' href={"/docs/admin/playbook"}>
        Built-in playbooks in Pigsty
    </Card>
    <Card icon={<LayoutDashboardIcon className="text-purple-600"/>} title='Dashboard' href={"/pgsql/dashboard"}>
        Introduction to grafana dashboards
    </Card>
    <Card icon={<ActivityIcon className="text-orange-500"/>} title='Monitor' href={"/pgsql/monitor"}>
        Introduction to prometheus & alertmanager
    </Card>
</Cards>

--------


<Cards>
    <Card icon={<DoorOpen className="text-violet-500"/>} title='Nginx Portal' href={"/docs/admin/portal"}>
        Nginx Portal for WebUI services
    </Card>
    <Card icon={<Warehouse className="text-amber-500"/>} title='Local Repo' href={"/docs/admin/repo"}>
        Manage local APT / YUM repository
    </Card>
    <Card icon={<GlobeIcon className="text-cyan-500"/>} title='Domain Name' href={"/docs/admin/domain"}>
        Use local / public domain names
    </Card>
    <Card icon={<ShieldCheckIcon className="text-rose-500"/>} title='CA & Cert' href={"/docs/admin/cert"}>
        Use self-signed or real HTTPS certificates
    </Card>
</Cards>


--------

<Cards>
    <Card icon={<Database className="text-sky-600"/>} title='PGSQL' href={"/pgsql/admin"}>
        PostgreSQL Cluster with HA, PITR, IaC, ACL, Monitoring, Pooling
    </Card>
    <Card icon={<Landmark className="text-green-700"/>} title='INFRA' href={"/infra/admin"}>
        Nginx, Repo, DNS, NTP, Prometheus and Grafana stack for Observability
    </Card>
    <Card icon={<CpuIcon className={"text-amber-500"}/>} title='NODE' href={"/node/admin"}>
        Enroll nodes into the desired state and monitor it, and VIP, HAProxy
    </Card>
    <Card icon={<Settings className={"text-sky-400"}/>} title='ETCD' href={"/etcd/admin"}>
        Reliable distributed consensus storage (DCS), empowering PGSQL HA
    </Card>
    <Card icon={<Box className="text-red-800"/>} title='MINIO' href={"/minio/admin"}>
        S3 compatible object storage, optional backup storage
    </Card>
    <Card icon={<Layers className="text-red-400"/>} title='REDIS' href={"/redis/admin"}>
        High-performance in-memory cache, optional data structure server
    </Card>

</Cards>