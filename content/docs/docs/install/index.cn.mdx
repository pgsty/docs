---
title: 安装
description: 快速上手安装 Pigsty
icon: Rocket
full: true
---

import {
    Database,
    CpuIcon,
    SearchIcon,
    FileCode,
    Code,
    AtomIcon,
    ToyBrickIcon,
} from 'lucide-react';

import { Step, Steps } from 'fumadocs-ui/components/steps';



## 快速上手

[**Quick start**](/docs/install/start): [Prepare](/docs/prepare) an [ssh-accessible](/docs/prepare/admin#check-accessibility) [node](/docs/prepare/hardware#node) with fresh installed [Linux](/docs/prepare/linux),
run as [user](/docs/prepare/admin#user) with nopass [`ssh`](/docs/prepare/admin#ssh) and [`sudo`](/docs/prepare/admin#sudo-privileges) privileges:

<Steps>
    <Step>

        [**Download**](/docs/prepare/software#pigsty) pigsty with:

        ```bash
        curl -fsSL https://repo.pigsty.io/get | bash; cd ~/pigsty;
        ```

    </Step>

    <Step>
        [**Configure**](/docs/config/configure) the [inventory](/docs/config/inventory) file according to your need. (<span className={"text-red-500 font-black"}>CHANGE PASSWORDS</span> in the generated [`pigsty.yml`](/docs/config/inventory))

        ```bash
        ./configure
        ```

    </Step>
    <Step>

        [**Install**](/docs/install/start) everything according to your config:

        ```bash
        ./install.yml
        ```
    </Step>


</Steps>


Pigsty Installed!  Explore the [**User Interface**](/docs/install/interface), check port `5432` for [Postgres](/pgsql) [Service](/pgsql/service), port `3000` for [Grafana](/docs/install/interface#grafana) dashboards (`admin` / `pigsty`).



--------

## 下一步？

- [访问 Postgres 服务](/docs/install/interface#postgresql)
- [探索 Grafana 监控面板](/docs/install/interface#grafana)
- [创建业务数据库](/pgsql/db#create-database) 与 [业务用户](/pgsql/user#create-user)
- [使用更多节点构建高可用部署](/docs/install/multinode)
- [加装更多 PostgreSQL 扩展](/pgsql/extension)


--------

## 安装


<Cards>
    <Card icon={<Database className="text-blue-300" />} title='单节点安装' href={"/docs/install/start"}>
        标准单机安装，在一台 Linux 服务器上从零开始部署一切组件。
    </Card>
    <Card icon={<AtomIcon className="text-green-600" />} title='多节点安装' href={"/docs/install/multinode"}>
        使用更多节点打造真高可用部署，使用本地软件仓库确保版本一致性。
    </Card>
    <Card icon={<SearchIcon className={"text-orange-400"} />} title='离线安装' href={"/docs/install/offline"}>
        在没有互联网的环境中，从离线安装包安装所有组件
    </Card>
    <Card icon={<ToyBrickIcon className={"text-red-500"} />} title='精简安装' href={"/docs/install/minimal"}>
        只安装高可用 Postgres 集群，不带监控
    </Card>
</Cards>


--------

## 准备

<Cards>

    <Card icon={<Database className="text-blue-300" />} title='Prepare' href={"/docs/prepare"}>
        Prepare node, network, storage, user, ssh, sudo, ansible, etc...
    </Card>

    <Card icon={<ToyBrickIcon className={"text-red-500"} />} title='Linux OS' href={"/docs/prepare/linux"}>
        Check compatible linux distributions and availability matrix
    </Card>

    <Card icon={<AtomIcon className="text-green-600" />} title='Vagrant' href={"/docs/prepare/vagrant"}>
        Provision local linux virtual machines with vagrant / virtualbox
    </Card>
    <Card icon={<SearchIcon className={"text-orange-400"} />} title='Terraform' href={"/docs/prepare/terraform"}>
        Provision cloud linux servers with terraform on cloud vendors
    </Card>

</Cards>


--------

## 参考

<Cards>

    <Card icon={<SearchIcon className={"text-orange-400"} />} title='Interface' href={"/docs/install/interface"}>
        Provision cloud linux servers with terraform on cloud vendors
    </Card>

    <Card icon={<Database className="text-blue-300" />} title='Configure' href={"/docs/config/configure"}>
        Prepare node, network, storage, user, ssh, sudo, ansible, etc...
    </Card>

    <Card icon={<ToyBrickIcon className={"text-red-500"} />} title='Playbook' href={"/docs/admin/playbook"}>
        Check compatible linux distributions and availability matrix
    </Card>

    <Card icon={<AtomIcon className="text-red-600" />} title='Security' href={"/docs/config/security"}>
        Security hardening and best practices for production environments
    </Card>


</Cards>