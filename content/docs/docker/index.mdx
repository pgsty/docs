---
title: Docker
description: Docker, the open container service
icon: Container
---

import {
    CodeXml,
    Variable,
    Wrench,
    ScrollText,
    Telescope,
    MessageCircleQuestion
} from 'lucide-react';


Docker is an [optional module](/docs/intro/module#extra-modules) in Pigsty, it is downloaded by default but not installed.
You have to explicitly [enable](/docker/config) it before using.

<Cards>
    <Card icon={<CodeXml className="text-sky-700"/>} title='Configuration' href={"/docker/config"}>
        Configure docker registry, proxy, images, etc...
    </Card>
    <Card icon={<Variable className="text-amber-500"/>} title='Parameters' href={"/docker/param"}>
        Customize docker components with 8 parameters
    </Card>
    <Card icon={<Wrench className={"text-violet-500"}/>} title='Administration' href={"/docker/admin"}>
        Manage docker images, containers, etc...
    </Card>
    <Card icon={<ScrollText className="text-blue-400"/>} title='Playbooks' href={"/docker/playbook"}>
        Ansible playbooks that can be used in docker module
    </Card>
    <Card icon={<Telescope className="text-orange-600"/>} title='Monitoring' href={"/docker/monitor"}>
        Dashboards, metrics, record & alerting rules.
    </Card>
    <Card icon={<MessageCircleQuestion className="text-emerald-600"/>} title='FAQ' href={"/docker/faq"}>
        Frequently Asked Questions about docker module
    </Card>
</Cards>
