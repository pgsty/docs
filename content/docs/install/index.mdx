---
title: Setup
description: Getting started with Pigsty
icon: Rocket
full: true
---

import {
    Database,
    CpuIcon,
    SearchIcon,
    FileCode,
    AtomIcon,
    ToyBrickIcon,
} from 'lucide-react';

import { Step, Steps } from 'fumadocs-ui/components/steps';

## Quick Start

[Prepare](/docs/prepare) one [ssh-accessible](/docs/prepare#accessibility) [node](/docs/prepare#node) with fresh installed [Compatible Linux OS](/docs/prepare/linux),
 run as user with nopass [`ssh`](/docs/prepare#ssh) and [`sudo`](/docs/prepare#admin-user) privileges:

<Steps>
<Step>

[**Download**](/docs/install#download) pigsty with:

```bash
curl -fsSL https://repo.pigsty.io/get | bash; cd ~/pigsty;
```

</Step>
<Step>
[Bootstrap](/docs/install/offline#bootstrap) pigsty with required dependencies (`ansible`):

```bash
./bootstrap
```
</Step>

<Step>
[**Configure**](/docs/install#configure) the [`pigsty.yml`](/docs/config/inventory) inventory file according your need and environment.

```bash
./configure
```

</Step>
<Step>

[**Install**](/docs/install#install) everything accoring to your config:

```bash
./install.yml
```
</Step>


</Steps>


Pigsty Single-Node [**Installation**](/docs/install) Ready!
Check port `5432` for [postgres](/docs/pgsql) [service](/docs/pgsql/service), port `3000` for grafana dashboards (userpass: `admin` / `pigsty`).


--------

## What's Next?

- Access Postgres Service
- Explore Grafana Dashboards
- Adding more nodes for real HA
- Point-In-Time-Recovery / HA Drill
- Get some postgres extensions installed


--------

## Installation


<Cards>
    <Card icon={<Database className="text-blue-300" />} title='One-Node Installation' href={"/docs/install"}>
        Setup current linux node, and download everything from the Internet
    </Card>
    <Card icon={<AtomIcon className="text-green-600" />} title='Multi-Node Installation' href={"/docs/install/multinode"}>
        Setup multiple nodes for real high-available production environment.
    </Card>
    <Card icon={<SearchIcon className={"text-orange-400"} />} title='Offline Installation' href={"/docs/install/offline"}>
        Bootstrap from a local offline package which contains all the packages.
    </Card>
    <Card icon={<ToyBrickIcon className={"text-red-500"} />} title='Minimal Installation' href={"/docs/install/minimal"}>
        Install essential packages for HA Postgres Cluster only, no monitoring.
    </Card>
</Cards>


--------

## Preparation

<Cards>

    <Card icon={<Database className="text-blue-300" />} title='Prepare' href={"/docs/prepare"}>
        Prepare node, network, storage, user, ssh, sudo, ansible, etc...
    </Card>

    <Card icon={<ToyBrickIcon className={"text-red-500"} />} title='Linux OS' href={"/docs/prepare/linux"}>
        Check compatible linux distributions and availability matrix
    </Card>

    <Card icon={<AtomIcon className="text-green-600" />} title='Vagrant' href={"/docs/prepare/vagrant"}>
        Provision local linux virtual machines with vagrant / virtualbox
    </Card>
    <Card icon={<SearchIcon className={"text-orange-400"} />} title='Terraform' href={"/docs/prepare/terraform"}>
        Provision cloud linux servers with terraform on cloud vendors
    </Card>

</Cards>

--------

## Reference

<Cards>

    <Card icon={<SearchIcon className={"text-orange-400"} />} title='Explore' href={"/docs/prepare/terraform"}>
        Provision cloud linux servers with terraform on cloud vendors
    </Card>

    <Card icon={<Database className="text-blue-300" />} title='Configure' href={"/docs/prepare"}>
        Prepare node, network, storage, user, ssh, sudo, ansible, etc...
    </Card>

    <Card icon={<ToyBrickIcon className={"text-red-500"} />} title='Playbook' href={"/docs/prepare/linux"}>
        Check compatible linux distributions and availability matrix
    </Card>

    <Card icon={<AtomIcon className="text-red-600" />} title='Security' href={"/docs/prepare/vagrant"}>
        Security hardening and best practices for production environments
    </Card>


</Cards>