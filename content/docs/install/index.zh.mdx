---
title: 安装
description: 快速上手安装 Pigsty
icon: Rocket
full: true
---

import {
    Database,
    CpuIcon,
    SearchIcon,
    FileCode,
    Code,
    AtomIcon,
    ToyBrickIcon,
} from 'lucide-react';

import { Step, Steps } from 'fumadocs-ui/components/steps';



## 快速上手

[**Quick start**](/install/start): [Prepare](/prepare) an [ssh-accessible](/prepare/admin#check-accessibility) [node](/prepare/hardware#node) with fresh installed [Linux](/prepare/linux),
run as [user](/prepare/admin#user) with nopass [`ssh`](/prepare/admin#ssh) and [`sudo`](/prepare/admin#sudo-privileges) privileges:

<Steps>
    <Step>

        [**Download**](/prepare/software#pigsty) pigsty with:

        ```bash
        curl -fsSL https://repo.pigsty.io/get | bash; cd ~/pigsty;
        ```

    </Step>

    <Step>
        [**Configure**](/config/configure) the [inventory](/config/inventory) file according to your need. (<span className={"text-red-500 font-black"}>CHANGE PASSWORDS</span> in the generated [`pigsty.yml`](/config/inventory))

        ```bash
        ./configure
        ```

    </Step>
    <Step>

        [**Install**](/install/start) everything according to your config:

        ```bash
        ./install.yml
        ```
    </Step>


</Steps>


Pigsty Installed!  Explore the [**User Interface**](/install/interface), check port `5432` for [Postgres](/pgsql) [Service](/pgsql/service), port `3000` for [Grafana](/install/interface#grafana) dashboards (`admin` / `pigsty`).



--------

## 下一步？

- [访问 Postgres 服务](/install/interface#postgresql)
- [探索 Grafana 监控面板](/install/interface#grafana)
- [创建业务数据库](/pgsql/db#create-database) 与 [业务用户](/pgsql/user#create-user)
- [使用更多节点构建高可用部署](/install/multinode)
- [加装更多 PostgreSQL 扩展](/pgsql/extension)


--------

## 安装


<Cards>
    <Card icon={<Database className="text-blue-300" />} title='单节点安装' href={"/install/start"}>
        标准单机安装，在一台 Linux 服务器上从零开始部署一切组件。
    </Card>
    <Card icon={<AtomIcon className="text-green-600" />} title='多节点安装' href={"/install/multinode"}>
        使用更多节点打造真高可用部署，使用本地软件仓库确保版本一致性。
    </Card>
    <Card icon={<SearchIcon className={"text-orange-400"} />} title='离线安装' href={"/install/offline"}>
        在没有互联网的环境中，从离线安装包安装所有组件
    </Card>
    <Card icon={<ToyBrickIcon className={"text-red-500"} />} title='精简安装' href={"/install/minimal"}>
        只安装高可用 Postgres 集群，不带监控
    </Card>
</Cards>


--------

## 准备

<Cards>

    <Card icon={<Database className="text-blue-300" />} title='Prepare' href={"/prepare"}>
        Prepare node, network, storage, user, ssh, sudo, ansible, etc...
    </Card>

    <Card icon={<ToyBrickIcon className={"text-red-500"} />} title='Linux OS' href={"/prepare/linux"}>
        Check compatible linux distributions and availability matrix
    </Card>

    <Card icon={<AtomIcon className="text-green-600" />} title='Vagrant' href={"/prepare/vagrant"}>
        Provision local linux virtual machines with vagrant / virtualbox
    </Card>
    <Card icon={<SearchIcon className={"text-orange-400"} />} title='Terraform' href={"/prepare/terraform"}>
        Provision cloud linux servers with terraform on cloud vendors
    </Card>

</Cards>


--------

## 参考

<Cards>

    <Card icon={<SearchIcon className={"text-orange-400"} />} title='Interface' href={"/install/interface"}>
        Provision cloud linux servers with terraform on cloud vendors
    </Card>

    <Card icon={<Database className="text-blue-300" />} title='Configure' href={"/config/configure"}>
        Prepare node, network, storage, user, ssh, sudo, ansible, etc...
    </Card>

    <Card icon={<ToyBrickIcon className={"text-red-500"} />} title='Playbook' href={"/admin/playbook"}>
        Check compatible linux distributions and availability matrix
    </Card>

    <Card icon={<AtomIcon className="text-red-600" />} title='Security' href={"/config/security"}>
        Security hardening and best practices for production environments
    </Card>


</Cards>