---
title: Setup
description: Getting started with Pigsty
icon: Rocket
full: true
---

import {
    Database,
    CpuIcon,
    SearchIcon,
    FileCode,
    AtomIcon,
    ToyBrickIcon,
} from 'lucide-react';


import { DynamicLink } from 'fumadocs-core/dynamic-link';

import { Step, Steps } from 'fumadocs-ui/components/steps';

import { File, Folder, Files } from 'fumadocs-ui/components/files';


## Quick Start

[Prepare](/docs/setup/prepare) a [ssh-accessible](/docs/setup/prepare#accessibility) [node](/docs/setup/prepare#node) with fresh installed [Compatible Linux OS](/docs/setup/linux),
 run as user with nopass [`ssh`](/docs/setup/prepare#ssh) and [`sudo`](/docs/setup/prepare#admin-user) privileges:

<Steps>
<Step>

[**Download**](/docs/setup/install#download) pigsty with:

```bash
curl -fsSL https://repo.pigsty.io/get | bash; cd ~/pigsty;
```

</Step>
<Step>
[Bootstrap](/docs/setup/offline#bootstrap) pigsty with required dependencies (`ansible`):

```bash
./bootstrap
```
</Step>

<Step>
[**Configure**](/docs/setup/install#configure) the [`pigsty.yml`](/docs/setup/config) inventory file according your need and environment.

```bash
./configure
```

</Step>
<Step>

[**Install**](/docs/setup/install#install) everything accoring to your config:

```bash
./install.yml
```
</Step>


</Steps>


Pigsty Single-Node [**Installation**](/docs/setup/install) Ready! 
Check port `5432` for [postgres](/docs/pgsql) [service](/docs/pgsql/service), port `3000` for grafana dashboards (userpass: `admin` / `pigsty`).


--------

## What's Next?

- Access Postgres Service
- Explore Grafana Dashboards
- Adding more nodes for real HA
- Point-In-Time-Recovery / HA Drill
- Get some postgres extensions installed


--------

## Installation


<Cards>
    <Card icon={<Database className="text-blue-300" />} title='Standard Installation' href={"/docs/setup/install"}>
        Download everything from the Internet and install latest packages.
    </Card>
    <Card icon={<AtomIcon className="text-green-600" />} title='Offline Installation' href={"/docs/setup/offline"}>
        Bootstrap from a local offline package which contains all the packages.
    </Card>
    <Card icon={<SearchIcon className={"text-orange-400"} />} title='Minimal Installation' href={"/docs/setup/slim"}>
        Install essential packages for HA Postgres Cluster only, no monitoring.
    </Card>
    <Card icon={<ToyBrickIcon className={"text-red-500"} />} title='Security Consideration' href={"/docs/setup/security"}>
        Security best-practices for your serious production deployment
    </Card>
</Cards>


--------

## Preparation

<Cards>
    <Card icon={<ToyBrickIcon className={"text-red-500"} />} title='Linux' href={"/docs/setup/linux"}>
        Check compatible linux distributions and availability matrix
    </Card>
    <Card icon={<Database className="text-blue-300" />} title='Prepare' href={"/docs/setup/prepare"}>
        Prepare node, network, storage, user, ssh, sudo, ansible, etc...
    </Card>

    <Card icon={<AtomIcon className="text-green-600" />} title='Vagrant' href={"/docs/setup/vagrant"}>
        Provion local linux virtual machines with vagrant / virtualbox
    </Card>
    <Card icon={<SearchIcon className={"text-orange-400"} />} title='Terraform' href={"/docs/setup/terraform"}>
        Provion cloud linux servers with terraform on cloud vendors
    </Card>

</Cards>

