---
title: PostgreSQL
description: 世界上最先进的开源关系型数据库！
icon: Database
---

import {
    Boxes,
    CodeXml,
    Package,
    Database,
    Users,
    ConciergeBell,
    KeySquare,
    Fingerprint,
    Wrench,
    DatabaseBackup,
    Telescope,
    Truck,
    Variable,
    ScrollText,
    CircleGauge,
    MessageCircleQuestion,
    Cpu
} from 'lucide-react';


## 概念 [#concept]

了解 Pigsty 中的 PostgreSQL 集群架构，重要实体与核心概念。

<Cards>
    <Card icon={<Boxes className="text-red-600"/>} title='架构' href={"/zh/pgsql/arch"}>
        PostgreSQL 集群架构与核心概念
    </Card>
    <Card icon={<ConciergeBell className="text-orange-500"/>} title='服务' href={"/zh/pgsql/service"}>
        通过负载均衡、代理、连接池提供可靠服务接入
    </Card>
    <Card icon={<Database className="text-blue-500"/>} title='数据库' href={"/zh/pgsql/db"}>
        定义、创建、管理业务数据库
    </Card>
    <Card icon={<Users className="text-indigo-600"/>} title='用户' href={"/zh/pgsql/user"}>
        定义、创建、管理业务用户与角色
    </Card>
    <Card icon={<KeySquare className="text-teal-600"/>} title='认证' href={"/zh/pgsql/hba"}>
        使用 HBA 规则进行认证与访问控制
    </Card>
    <Card icon={<Fingerprint className="text-purple-600"/>} title='权限' href={"/zh/pgsql/acl"}>
        开箱即用的默认角色与权限模型
    </Card>
</Cards>


------

## 管理 [#administration]

<Cards>
    <Card icon={<Cpu className="text-cyan-600"/>} title='内核' href={"/zh/pgsql/kernel"}>
        使用不同风味的 PostgreSQL 内核分支
    </Card>
    <Card icon={<Package className="text-emerald-600"/>} title='扩展' href={"/zh/pgsql/extension"}>
        利用 437 个 PostgreSQL 扩展协同带来的超能力
    </Card>
    <Card icon={<CodeXml className="text-sky-700"/>} title='配置' href={"/zh/pgsql/config"}>
        定义不同类型的 PostgreSQL 实例和集群
    </Card>
    <Card icon={<Variable className="text-amber-500"/>} title='参数' href={"/zh/pgsql/param"}>
        使用 120 个参数来深度定制 PostgreSQL 集群
    </Card>
    <Card icon={<Wrench className="text-violet-500"/>} title='管理' href={"/zh/pgsql/admin"}>
        管理 PostgreSQL 集群、实例、用户、数据库
    </Card>
    <Card icon={<ScrollText className="text-blue-400"/>} title='剧本' href={"/zh/pgsql/playbook"}>
        使用 Ansible 剧本进行控制原语操作
    </Card>
    <Card icon={<DatabaseBackup className="text-green-600"/>} title='备份恢复' href={"/zh/pgsql/backup"}>
        备份与时间点恢复 (PITR)
    </Card>
    <Card icon={<Truck className="text-pink-600"/>} title='迁移' href={"/zh/pgsql/migration"}>
        零停机蓝绿部署迁移
    </Card>
    <Card icon={<Telescope className="text-orange-600"/>} title='监控' href={"/zh/pgsql/monitor"}>
        监控现有的 PostgreSQL 或 RDS 实例
    </Card>
    <Card icon={<CircleGauge className="text-yellow-600"/>} title='仪表板' href={"/zh/pgsql/dashboard"}>
        使用 Grafana 仪表板可视化信息
    </Card>

</Cards>

