---
title: PostgreSQL
description: The most advanced open source relational database in the world!
icon: Database
---

import {
    Boxes,
    CodeXml,
    Package,
    Database,
    Users,
    ConciergeBell,
    KeySquare,
    Fingerprint,
    Wrench,
    DatabaseBackup,
    Telescope,
    Truck,
    Variable,
    ScrollText,
    CircleGauge,
    MessageCircleQuestion,
    Cpu
} from 'lucide-react';


## Concept

<Cards>
    <Card icon={<Boxes className="text-red-600"/>} title='Architecture' href={"/pgsql/arch"}>
        PostgreSQL cluster architecture and concept
    </Card>
    <Card icon={<ConciergeBell className="text-orange-500"/>} title='Service' href={"/pgsql/service"}>
        Reliable service access via lb, proxy, pool
    </Card>
    <Card icon={<Database className="text-blue-500"/>} title='Database' href={"/pgsql/db"}>
        Define, create, and manage business databases
    </Card>
    <Card icon={<Users className="text-indigo-600"/>} title='User Role' href={"/pgsql/user"}>
        Define, create, and manage users and roles
    </Card>
    <Card icon={<KeySquare className="text-teal-600"/>} title='Auth / HBA' href={"/pgsql/hba"}>
        Host-Based Authentication in Pigsty
    </Card>
    <Card icon={<Fingerprint className="text-purple-600"/>} title='Privileges' href={"/pgsql/acl"}>
        Access Control with default roles and privileges
    </Card>
</Cards>

------

## Administration

<Cards>
    <Card icon={<Cpu className="text-cyan-600"/>} title='Kernel' href={"/pgsql/kernel"}>
        Replace vanilla PostgreSQL with exotic kernel forks
    </Card>
    <Card icon={<Package className="text-emerald-600"/>} title='Extension' href={"/pgsql/extension"}>
        Harness the synergistic power of PostgreSQL extensions
    </Card>
    <Card icon={<CodeXml className="text-sky-700"/>} title='Configure' href={"/pgsql/config"}>
        Describe and configure PostgreSQL clusters
    </Card>
    <Card icon={<Variable className="text-amber-500"/>} title='Parameter' href={"/pgsql/param"}>
        Customize postgres cluster with 120 parameters
    </Card>
    <Card icon={<Wrench className="text-violet-500"/>} title='Administration' href={"/pgsql/admin"}>
        Run administrative tasks on PostgreSQL clusters
    </Card>
    <Card icon={<ScrollText className="text-blue-400"/>} title='Playbook' href={"/pgsql/playbook"}>
        Control primitives with Ansible playbooks
    </Card>
    <Card icon={<DatabaseBackup className="text-green-600"/>} title='Backup & PITR' href={"/pgsql/backup"}>
        Backup and point-in-time recovery
    </Card>
    <Card icon={<Truck className="text-pink-600"/>} title='Migration' href={"/pgsql/migration"}>
        Zero-downtime blue-green deployment
    </Card>
    <Card icon={<Telescope className="text-orange-600"/>} title='Monitor' href={"/pgsql/monitor"}>
        Monitor existing PostgreSQL or RDS
    </Card>
    <Card icon={<CircleGauge className="text-yellow-600"/>} title='Dashboard' href={"/pgsql/dashboard"}>
        Visualized information with Grafana dashboards
    </Card>
</Cards>

